# 📤 Публикация KyrLat на GitHub

## Шаг 1: Создайте репозиторий на GitHub

1. Откройте [github.com](https://github.com) и войдите в аккаунт
2. Нажмите **"+"** → **"New repository"**
3. Заполните:
   - **Repository name:** `KyrLat`
   - **Description:** `Uzbek Cyrillic ↔ Latin transliterator`
   - **Public** или **Private** (на ваш выбор)
   - **НЕ** ставьте галочки на "Add README" и "Add .gitignore"
4. Нажмите **"Create repository"**

---

### Шаг 2: Создание EXE

В папке `d:\projects\KyrLat` выполните:

```bash
pyinstaller --onefile --windowed --icon=icon.ico --add-data "icon.ico;." --name=KyrLat main.py
```

**Параметры:**
- `--onefile` — один EXE файл (не папка с кучей файлов)
- `--windowed` — без консоли (чтобы не мелькало черное окно)
- `--icon=icon.ico` — иконка EXE файла в проводнике
- `--add-data "icon.ico;."` — включает icon.ico внутрь EXE для окна приложения
- `--name=KyrLat` — имя итогового файла

---

## Шаг 3: Инициализируйте Git локально

Откройте PowerShell в папке `d:\projects\KyrLat` и выполните:

```bash
# Инициализация Git репозитория
git init

# Добавление всех файлов
git add .

# Первый коммит
git commit -m "Initial commit: KyrLat v1.0"
```

---

## Шаг 4: Подключите удалённый репозиторий

Замените `yourusername` на ваш GitHub username:

```bash
git remote add origin https://github.com/yourusername/KyrLat.git
```

---

## Шаг 4: Отправьте код на GitHub

```bash
# Отправка кода
git branch -M main
git push -u origin main
```

Если попросит логин/пароль — используйте **Personal Access Token** вместо пароля.

---

## Шаг 5: Создайте Release с EXE файлом

1. На странице репозитория нажмите **"Releases"** → **"Create a new release"**
2. Заполните:
   - **Tag version:** `v1.0.0`
   - **Release title:** `KyrLat v1.0.0 - Initial Release`
   - **Description:** 
     ```
     First stable release of KyrLat!
     
     Features:
     - Live transliteration
     - Auto-detection
     - Clean modern UI
     - Character counter
     ```
3. Прикрепите файл: перетащите `dist/KyrLat.exe` в поле **"Attach binaries"**
4. Нажмите **"Publish release"**

---

## Шаг 6: Добавьте скриншот (опционально)

1. Запустите программу и сделайте скриншот
2. Сохраните как `screenshot.png` в папке проекта
3. Добавьте и закоммитьте:
   ```bash
   git add screenshot.png
   git commit -m "Add screenshot"
   git push
   ```

---

## ✅ Готово!

Ваш проект теперь опубликован: `https://github.com/yourusername/KyrLat`

### Не забудьте обновить в README.md:
- Ссылку `yourusername` на ваш реальный GitHub username
- Добавить скриншот программы

---

## 🔄 Обновление проекта в будущем

Когда внесёте изменения:

```bash
git add .
git commit -m "Описание изменений"
git push
```
